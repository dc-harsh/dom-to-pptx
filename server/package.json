{
  "name": "dom-to-pptx-server",
  "version": "1.0.0",
  "description": "HTTP API server to convert HTML to PPTX",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "playwright": "^1.58.0"
  }
}
